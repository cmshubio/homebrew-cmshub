**cmshub** cli installation, first install [Homebrew](https://brew.sh/), next execute:

```
$ brew tap cmshubio/cmshub
$ brew cask install cmshub
```

Update *cmshub* cli:

```
$ brew update
$ brew cask reinstall cmshub
```
